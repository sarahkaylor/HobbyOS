# HobbyOS

A simple C language project created as a starting point for an OS.

## Setup
Requires `clang` (installed with Xcode).

## Build
Run `make` to compile the project.

## Run
Execute the resulting binary: `./hobbyos`
